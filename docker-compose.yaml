version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: mini-konveksi-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: konveksi_db
      POSTGRES_USER: konveksi_user
      POSTGRES_PASSWORD: konveksi_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - mini-konveksi-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U konveksi_user -d konveksi_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mini-konveksi-app
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://konveksi_user:konveksi_password@postgres:5432/konveksi_db
      - BETTER_AUTH_SECRET=your-super-secret-key-change-in-production
      - BETTER_AUTH_URL=http://localhost:3000
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - mini-konveksi-network

  nginx:
    image: nginx:alpine
    container_name: mini-konveksi-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/nginx/sites-available/mini-konveksi:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/www/html:/var/www/html:ro
    depends_on:
      - app
    networks:
      - mini-konveksi-network

  postgres-dev:
    image: postgres:16-alpine
    container_name: mini-konveksi-postgres-dev
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5433:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    networks:
      - mini-konveksi-network
    profiles:
      - dev

volumes:
  postgres_data:
    driver: local
  postgres_dev_data:
    driver: local

networks:
  mini-konveksi-network:
    driver: bridge